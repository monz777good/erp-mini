generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  SALES
  ADMIN
}

enum OrderStatus {
  REQUESTED
  APPROVED
  REJECTED
  DONE
}

model User {
  id        String   @id @default(cuid())
  name      String
  phone     String   @unique
  role      Role     @default(SALES)
  createdAt DateTime @default(now())

  orders    Order[]
  clients   Client[] // ✅ 추가: 내가 만든 거래처들
}

model Item {
  id        String   @id @default(cuid())
  name      String   @unique
  createdAt DateTime @default(now())

  orders    Order[]
}

model Client {
  id           String   @id @default(cuid())
  name         String
  bizRegNo     String?  @unique
  receiverName String?
  receiverAddr String?
  receiverTel  String?
  receiverMob  String?
  memo         String?
  createdAt    DateTime @default(now())

  // ✅ 추가: 거래처 소유 영업사원 (기존 데이터 대비해서 nullable)
  salesId      String?
  sales        User?    @relation(fields: [salesId], references: [id])

  orders       Order[]

  @@index([salesId])
}

model Order {
  id          String      @id @default(cuid())
  quantity    Int
  status      OrderStatus @default(REQUESTED)
  note        String?
  createdAt   DateTime    @default(now())

  userId      String
  user        User        @relation(fields: [userId], references: [id])

  itemId      String
  item        Item        @relation(fields: [itemId], references: [id])

  clientId    String?
  client      Client?     @relation(fields: [clientId], references: [id])

  receiverName String
  receiverAddr String
  receiverTel  String?
  receiverMob  String?
  boxCount     Int        @default(1)
  shippingFee  Int        @default(3850)
  message      String?
}